# Copier vers `.env` : cp .env.example .env
# Chargé automatiquement hors production (voir src/main.ts).
# En prod : variables injectées via deploy/.env.production → docker-compose.

SMTP_HOST=ssl0.ovh.net
SMTP_PORT=465
SMTP_USER=contact@planwise.fr
SMTP_PASS=
SMTP_FROM=Planwise <contact@planwise.fr>
